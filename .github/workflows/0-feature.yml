name: Feature / Fix Branch
on:
  push:
     branches:
      - 'feature**'
      - 'fix**'

jobs:
 python-ci:
  uses: ./.github/workflows/python-ci.yml
  secrets: inherit #faz o python-ci acessar as secrets desse workflow principal